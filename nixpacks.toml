# Railway / Nixpacks — do NOT override install cmds (that skips Python/venv setup).
# Only add system libs for OpenCV headless and set the production start command.

[phases.install]
aptPkgs = ["libglib2.0-0", "libgomp1", "libgl1"]

[start]
cmd = "uvicorn app:app --host 0.0.0.0 --port $PORT"
