[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# Use Dockerfile CMD (start.sh) — do not set startCommand with raw $PORT (not expanded)
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
